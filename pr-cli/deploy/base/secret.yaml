apiVersion: v1
kind: Secret
metadata:
  name: pr-cli-secrets
type: Opaque
stringData:
  # IMPORTANT: These are placeholder values
  # Override these in your overlay or use external secret management
  WEBHOOK_SECRET: "changeme-webhook-secret"
  PR_TOKEN: "changeme-github-token"
  
  # Optional: Uncomment and set if needed
  # PR_COMMENT_TOKEN: "changeme-comment-token"
  # ALLOWED_REPOS: "myorg/*"
  # PR_ROBOT_ACCOUNTS: "bot1,bot2"
  # PR_LGTM_PERMISSIONS: "write,admin"

