apiVersion: apps/v1
kind: Deployment
metadata:
  name: pr-cli-webhook
spec:
  template:
    spec:
      containers:
      - name: pr-cli
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: 1000m
            memory: 1Gi

