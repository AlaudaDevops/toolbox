description: do not inject verify hooks for non matching repository

repoURL: https://github.com/alauda/idp-console.git

existingHooks:
  preVerifyScan:
    enabled: false

  postVerifyScan:
    enabled: false

rules:
  - name: tektoncd verify hooks
    when:
      repoNameGlob: "tektoncd-*"
    strategy: fillEmpty
    hooks:
      preVerifyScan:
        enabled: true
        script: |
          #!/bin/bash
          set -ex
          make clean-patches-default apply-patches-default upgrade-go-dependencies-default

      postVerifyScan:
        enabled: true
        script: |
          #!/bin/bash
          set -ex
          make clean-patches-default
